\documentclass[12pt,reqno, twoside]{amsbook}

\usepackage{csquotes} % needed for biblatex
\usepackage{preamble}

\setcounter{secnumdepth}{2}
\setcounter{tocdepth}{2}

\makeglossaries
\input{src/beforeIntro/acronyms.tex} % your acronyms definitions file
\addbibresource{references.bib} % Specify the path to your .bib file

% ============================================================
% Fast partial builds via \include / \includeonly
% ============================================================
% Detect whether we're being built by the Nix flake, which sets the
% THESIS_FULLBUILD environment variable before invoking latexmk. If
% it's set, we ignore \includeonly below and compile everything.
\makeatletter
\newif\iffullbuild
\directlua{
  if os.getenv("THESIS_FULLBUILD") then
    tex.print("\string\\fullbuildtrue")
  else
    tex.print("\string\\fullbuildfalse")
  end
}
\makeatother

% Manual local control: comment/uncomment entries to work on just one
% (or a few) chapters at a time. Files NOT listed here are skipped
% entirely during compilation (only their .aux data is reused), which
% is what makes this fast. This list is ignored whenever
% \fullbuild is true (i.e. always, when built via the Nix flake).
\iffullbuild
  % Full build requested (Nix) -- do not restrict, compile everything.
\else
  \includeonly{
    %src/introduction,
    %src/theoredical_background/theoredical_background,
    src/research_methodology/research_methodology,
    %src/literature_review/literature_review,
    %src/experiments/evaluation_of_alignment_faking,
    %src/conclusions,
    %src/appendix/appendix,
  }
\fi

\begin{document}

\input{src/beforeIntro/cover/cover.tex}

% Use the ISTA cover, you need to convert it from word
% \input{src/cover.tex} a

\thispagestyle{empty}

\begin{toreview}

\input{src/beforeIntro/dedication.tex}
\input{src/beforeIntro/epigraph.tex} 

\setcounter{page}{1}
\pagenumbering{roman} %

\frontmatter
\input{src/beforeIntro/acknowledgements.tex}

\end{toreview}

\input{src/beforeIntro/abstract.tex}
\input{src/beforeIntro/resumo.tex}

\setcounter{tocdepth}{2}
\tableofcontents
\listoffigures        % List of Figures
\listoftables         % List of Tables
\lstlistoflistings    % List of Listings (code)
\printglossary[type=\acronymtype, title=Acronyms, toctitle=Acronyms]

\mainmatter

\glsresetall

% In the end, if there are words being broken weirdly, you can tell latex how to break them here:
% \hyphenation{mis-align-ment su-per-in-tel-li-gence}

\setcounter{page}{1} \pagenumbering{arabic}

\include{src/introduction}
\include{src/theoredical_background/theoredical_background}
\include{src/research_methodology/research_methodology}
\include{src/literature_review/literature_review}
\include{src/experiments/evaluation_of_alignment_faking}
\include{src/conclusions}

\printbibliography

\include{src/appendix/appendix}

\end{document}